
services:
  nginx:
    image: nginx:latest
    container_name: home
    ports:
      - "8000:80"
    restart: always
    volumes:
      - ./home:/usr/share/nginx/html:ro
      # The course is on GitHub Pages (the landing page links there) — not served
      # by the stack, so no training/site mount. Preview locally with `make docs-serve`.
    depends_on:
      - airflow-apiserver
      - superset
      - trino
      - jupyter
      - spark-master
      - minio
    networks:
      - sparknet