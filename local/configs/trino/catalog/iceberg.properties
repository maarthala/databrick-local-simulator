# The `iceberg` catalog — backed by Apache Polaris (governed Iceberg REST).
# Name kept as `iceberg` so all course SQL (iceberg.bronze.*, iceberg.gold.*) is
# unchanged. Trino connects as root (admin) and reads/writes MinIO directly; Polaris
# governs the metadata. Per-persona enforcement is shown via Spark/Console logins.
connector.name=iceberg
iceberg.catalog.type=rest
iceberg.rest-catalog.uri=http://polaris:8181/api/catalog
iceberg.rest-catalog.warehouse=polaris_lake
iceberg.rest-catalog.security=OAUTH2
iceberg.rest-catalog.oauth2.credential=root:s3cr3t
iceberg.rest-catalog.oauth2.scope=PRINCIPAL_ROLE:ALL
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.path-style-access=true
s3.region=us-east-1
s3.aws-access-key=minioadmin
s3.aws-secret-key=minioadmin
