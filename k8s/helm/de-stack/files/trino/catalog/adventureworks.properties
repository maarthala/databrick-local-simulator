# AdventureWorks (OLTP) sample DB in the in-cluster Postgres — query from Trino/Metabase/Superset.
# Loaded by the adventureworks-load Job (see templates/adventureworks.yaml).
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/adventureworks
connection-user=postgres
connection-password=postgres
