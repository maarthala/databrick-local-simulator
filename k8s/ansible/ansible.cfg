[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
roles_path = roles
# Keep the whole build/deploy readable in one stream.
display_skipped_hosts = False

[ssh_connection]
pipelining = True
